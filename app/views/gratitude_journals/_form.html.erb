<%= form_with(model: gratitude_journal, local: true) do |form| %>
  <% if gratitude_journal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gratitude_journal.errors.count, "error") %> prohibited this gratitude_journal from being saved:</h2>

      <ul>
      <% gratitude_journal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit(class: "button") %>
  </div>
<% end %>
